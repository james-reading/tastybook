- unless @search.last_page?
  = link_to 'Load more',
    url_for(params.to_unsafe_h.merge(page: @search.page + 1)),
    class: 'btn btn-outline-primary js-load-more-recipes',
    remote: true,
    data: { 'disable-with': "<i class='fa fa-spinner fa-spin fa-fw line-height-base'></i>".html_safe }
